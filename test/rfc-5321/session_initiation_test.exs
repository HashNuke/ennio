defmodule SessionInitiationTest do
  use ExUnit.Case

  @tag :pending
  test "server responds with an opening message" do
  end

  @tag :pending
  test "may include identification of software version" do
  end

  @tag :pending
  test "can reject mail session with 554 replies instead of 220" do
  end


  @tag :pending
  test "554 replies should contain info for clients to debug" do
  end


  @tag :pending
  test "wait for client to quit rejected mail session with 503 replies" do
  end

end
